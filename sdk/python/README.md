# MIG Python SDK (Scaffold)

This directory contains the Python SDK scaffold for MIG v0.1.

Planned initial surface:
- `hello()`
- `discover()`
- `invoke()`

The Go SDK in `sdk/go/migclient` is currently the reference implementation.
